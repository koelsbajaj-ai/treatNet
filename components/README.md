# components

UI pieces used by `app/page.tsx` and later pages: the free-text intake
box (Stage 4), the extraction confirmation form with confidence badges
(Stage 4), the ranked results table (Stage 5), the persistent synthetic
-data / confounding-limitation banner (Stage 1 and 7), and the
provenance modal (Stage 8).
